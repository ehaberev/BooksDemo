//import { get } from '@ember/object';
import { helper } from '@ember/component/helper';
//import  ENV  from './config/environment';


export function env(params/*, hash*/) {
  return params;
}

export default helper(env);
